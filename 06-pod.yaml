apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: roboshop
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
  serviceAccountName: roboshop-secret-reader
  containers:
  - name: aws-cli # container
    image: amazon/aws-cli #by default it'll get gtom docker hub
    command: ["sleep", "1000"]